# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.venv/

# Secrets
secrets.cfg
*.env

# OS
.DS_Store
*.swp

# Loom stage fallbacks (crlfuzz URL lists, mini wordlists) must live
# under the run workdir, never the repo root — a bare-cwd fallback
# during ad-hoc/test runs once polluted the repo.
inputs/
